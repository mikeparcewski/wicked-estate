#!/usr/bin/env bash
# Enforce rustfmt before every commit. Install with:
#   git config core.hooksPath .githooks
set -euo pipefail

if ! cargo fmt --all -- --check 2>&1; then
  echo ""
  echo "  cargo fmt --all failed. Run: cargo fmt --all"
  echo ""
  exit 1
fi
