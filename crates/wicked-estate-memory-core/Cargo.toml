[package]
name = "wicked-estate-memory-core"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "Core types + differentiated logic for wicked-estate memory: tiers, scope, reasoner seam, salience/decay, recall rerank+budget, and the extended MemoryApi trait."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { version = "1", features = ["v7"] }
wicked-estate-core = { path = "../wicked-estate-core", version = "0.14.2" }
gaoya = "0.2.2"
