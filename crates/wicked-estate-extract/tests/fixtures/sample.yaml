name: wicked-estate
version: 0.1.0
description: Code intelligence graph engine
license: MIT
settings:
  debug: false
  workers: 4

# EG-COR-1 regression pin: a leading-colon key is a REAL name (legacy Rails
# database.yml symbol-key idiom) — the extractor must store it verbatim.
:adapter: postgres
