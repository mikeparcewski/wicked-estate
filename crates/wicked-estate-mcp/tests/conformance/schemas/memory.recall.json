{
  "captured_from": "wicked-memory-mcp-0.12.1",
  "description": "Conversational recall: the most relevant, token-budgeted slice for a query. Default visibility is inheritance (memories at `scope` or an ancestor of it); optional scope_prefix flips to subtree-inclusive visibility.",
  "inputSchema": {
    "properties": {
      "intent": {
        "additionalProperties": {
          "type": "string"
        },
        "description": "The session's intent tuple (axis->value). Recall returns the union of memories whose facets are a subset of this intent, specificity-ranked. Omitted => empty => only unfaceted memories surface.",
        "type": "object"
      },
      "query": {
        "type": "string"
      },
      "scope": {
        "type": "string"
      },
      "scope_prefix": {
        "description": "subtree filter: when present, candidates are memories whose scope equals the prefix or descends from it (same predicate as memory.erase/memory.coverage), replacing the inheritance filter; \"\" = every memory; omitted/null = inheritance behavior",
        "type": "string"
      },
      "seeds": {
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "token_budget": {
        "type": "integer"
      }
    },
    "required": [
      "query"
    ],
    "type": "object"
  },
  "name": "memory.recall"
}
